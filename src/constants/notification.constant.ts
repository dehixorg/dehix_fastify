// Base endpoint for notification-related operations
export const NOTIFICATION_ENDPOINT = "/notification";

// Endpoint for creating a new notification
export const NOTIFICATION_CREATE_ENDPOINT = "/createnotification";

// Endpoint for fetching a notification by its ID
export const NOTIFICATION_BY_ID_ENDPOINT = "/:notification_id";

// Endpoint for deleting a notification by its ID
export const NOTIFICATION_DELETE_BY_ID_ENDPOINT = "/:notification_id";

// Endpoint for fetching all notifications
export const NOTIFICATION_GET_ALL_ENDPOINT = "/all_notification";
