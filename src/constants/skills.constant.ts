// Base endpoint for managing skills
export const SKILLS_ENDPOINT = "/skills";

// Endpoint for retrieving all skills
export const SKILLS_ALL_ENDPOINT = "/all";

// Endpoint for deleting a specific skill by its ID
export const SKILLS_DELETE_BY_ID_ENDPOINT = "/:skill_id";

// Endpoint for creating a new skill
export const SKILLS_ID_ENDPOINT = "/createskill";

// Endpoint for retrieving a specific skill by its ID
export const SKILLS_BY_ID_ENDPOINT = "/:skill_id";
